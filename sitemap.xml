<?xml version="1.0" encoding="UTF-8"?>
<!-- The pages this repo's root serves. The playground is listed because
     nothing links to it yet: a crawler has no other way to reach it. The
     book keeps its own sitemap at /docs/sitemap.xml, generated by
     .github/workflows/docs.yml; robots.txt points at both.

     No lastmod in either file. The book's is rebuilt on a daily cron whose
     "commit only if the book changed" guard a build date would defeat, and
     this one follows the same rule so the pair reads alike. -->
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <url><loc>https://vilan-lang.org/</loc></url>
  <url><loc>https://vilan-lang.org/playground/</loc></url>
</urlset>
